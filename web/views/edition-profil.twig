{{ form_start(form) }}

{{ form_widget(form.username, {'attr': {'class':'test', 'value': userInfo.username }}) }}
{{ form_widget(form.avatar, {'attr': {'class':'test', 'value': userInfo.avatar }}) }}
<input type="submit" name="">
{{ form_end(form) }}